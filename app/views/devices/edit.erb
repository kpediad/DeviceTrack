<div class="inner-bg">
    <div class="container">

            <div class="row" style="margin-top: -20px">
              <div class="col-sm-6 col-sm-offset-3">

                <div class="form-box">
                    <div class="form-top">
                        <h3 style="color: #fff; text-align: center;">Edit Device</h3>
                        <% if flash.has?(:message) %>
                          <p style="text-align: center; color:#f0ad4e;"><%= flash[:message] %> </p>
                        <% end %>
                    </div>

                  <div class="form-bottom">
                    <form  action="/devices/<%=@device.id%>" method="post" >
                      <input type="hidden" id="hidden" name="_method" value="patch">
                      <div class="form-group">
                        <label class="sr-only" for="form-amount">Name</label>
                        <input type="text" name="name" value="<%=@device.name%>" class="form-amount form-control" id="form-amount" required>
                      </div>

                      <div class="form-group">
                        <label class="sr-only" for="form-description">Description</label>
                        <input type="text" name="description" value="<%=@device.description%>" class="form-username form-control" id="form-description" >
                      </div>


                      <div class="form-group">
                        <label  style="color: white; -webkit-font-smoothing: antialiased;" for="type">Select Existing Type: </label>
                        <select name="type_id">
                              <% @types.each do |type| %>
                                <option name="type[name]" value="<%= type.id %>" <%='selected' if @device.type.id == type.id%>><%= type.name %></option>
                              <%end%>
                              <option value="">Create New Type</option>
                        </select>
                      </div>

                      <div class="form-group" style="">
                        <input type="text" placeholder="or create a new device type here..." name="type[name]"/>
                      </div>


                        <button type="submit" class="btn btn-success">Edit Device</button>
                    </form>

                  </div>
              </div>
            </div>
    </div>
</div>
